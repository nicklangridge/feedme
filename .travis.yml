language: perl

perl: 
  - 5.26

addons:
  apt:
    packages:
      - libdbd-mysql-perl
      - libmysqlclient-dev

before_install:
  - cd server

install:
  - cpanm --installdeps --notest .
  
script: 
  - prove -v -I lib t/